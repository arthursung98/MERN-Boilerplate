// If trying to put it into production mode, uncomment this section.
// For now, we'll just use this in development mode.

// if(process.env.NODE_ENV === 'development') {
//     module.exports = require('./prod')
// } else {
//     module.exports = require('./dev')
// }

module.exports = require('./dev')